# YOLO + PostgreSQL + Flask Integration

Flask-based object detection app using YOLO and PostgreSQL. Includes REST endpoints for storing and querying results.

## Features
- YOLO detection
- Flask API
- PostgreSQL integration
- Simple and effective

## Tech Stack
- YOLO (Ultralytics)
- Flask
- PostgreSQL
- Python

## Use Case
Ideal for small-scale deployments and educational demos.

## Installation & Launch

1. Clone the repo:
   ```bash
   git clone https://github.com/Mazen-Ahmed12/integerating_YOLO_with_postgresql_and_flask.git
   cd integerating_YOLO_with_postgresql_and_flask

2. Install dependencies:
   ```bash
   pip install -r requirements.txt

3. Run Flask app:
   ```bash
   python app.py
4. you will find teh application running in bash and say
   - Running on http://127.0.0.1:5000
   - just open http://127.0.0.1:5000 to open the application webpage
